#!/usr/bin/node
// Declare two constant variables that store the first and second arguments passed
const arg1 = process.argv[2];
const arg2 = process.argv[3];

// Use a template literal to print the arguments in the desired format
console.log(`${arg1} is ${arg2}`);

