#!/usr/bin/node
// Declare a constant array that contains the 3 lines to print
const lines = ["C is fun", "Python is cool", "JavaScript is amazing"];

// Use a for loop to iterate over the array and print each line
for (let i = 0; i < lines.length; i++) {
  console.log(lines[i]);
}

